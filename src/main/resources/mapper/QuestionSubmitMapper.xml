<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dailw.mapper.QuestionSubmitMapper">

    <resultMap id="BaseResultMap" type="com.dailw.model.entity.QuestionSubmit">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="language" column="language" jdbcType="VARCHAR"/>
            <result property="code" column="code" jdbcType="VARCHAR"/>
            <result property="judgeInfo" column="judge_info" jdbcType="VARCHAR"/>
            <result property="status" column="status" jdbcType="INTEGER"/>
            <result property="questionId" column="question_id" jdbcType="BIGINT"/>
            <result property="userId" column="user_id" jdbcType="BIGINT"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
            <result property="competitionId" column="competition_id" jdbcType="BIGINT"/>
            <result property="deleted" column="deleted" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,language,code,
        judge_info,status,question_id,
        user_id,create_time,update_time,
        competition_id,deleted
    </sql>

    <select id="selectDailySubmitCount" resultType="java.util.Map">
        SELECT DATE(create_time) AS date, COUNT(*) AS count
        FROM question_submit
        WHERE create_time &gt;= DATE_SUB(CURDATE(), INTERVAL #{days} DAY)
          AND deleted = 0
        GROUP BY DATE(create_time)
        ORDER BY date ASC
    </select>

    <select id="selectStatusDistribution" resultType="java.util.Map">
        SELECT status, COUNT(*) AS count
        FROM question_submit
        WHERE deleted = 0
        GROUP BY status
    </select>
</mapper>
